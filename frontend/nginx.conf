server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: let React Router handle client-side routes like
    # /auth/callback and /shows/:id instead of nginx 404ing on a page reload.
    location / {
        try_files $uri /index.html;
    }
}
